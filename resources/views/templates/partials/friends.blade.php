<div>
	<a class="pull-left" href="{{ route('profile.index', $user->id) }}">
	    <img class="media-object profile-img mbotpad10" alt="{{ $user->getName() }}" 
	    src="{{ $user->profile_img }}" width="70">
	</a>
</div>
